{
  "name": "eleventy-graphic-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal, image-led graphic design portfolio built with Eleventy.",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
